const firstName='Rakib';
const lastName='Hossen';
const fullName=`${firstName} ${lastName}`
console.log(fullName);

//multiple line string

const lorem=`Must wait
Must wait
Must wait
Must wait
Must wait
`
console.log(lorem); 

