const computer ={
    brand : 'acer',
    price:4000,
    processor : 'Intel',
    ssd : "512gb"
}

const keys = Object.keys(computer);
console.log(keys);

const values=Object.values(computer)
console.log(values)